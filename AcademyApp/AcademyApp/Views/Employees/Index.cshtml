@model AcademyApp.Models.EmployeeIndex

<table>
    <tr>
        <th>Names</th>
    </tr>

    @for (int i = 0; i < Model.Names.Count; i++) {
        <tr>
            <td>@Model.Names[i]</td>
        </tr>
    }

</table>
